#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 1. 先修复语法错误
# echo "🔍 检查代码语法..."
# pnpm run fix

# 2. 格式化所有文件
echo "🎨 格式化代码..."
pnpm run format

# 3. 再次添加所有文件到暂存区
git add -A
